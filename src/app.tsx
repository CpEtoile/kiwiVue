import React from 'react';
import JsonViewer from './components/JsonViewer.tsx';

const App: React.FC = () => {
    return (
        <div>
            <JsonViewer />
        </div>
    );
};

export default App;
